spring:

  application:
    name: Greenbuy

  datasource:
    url: jdbc:postgresql://localhost:5432/Greenbuy
    username: postgres
    password: 2607
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    #      ddl-auto: none

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  main:
    allow-circular-references: true
server:
  port: 8080

